#ifndef GAME_RULES
#include <iostream>
#include <string>

using namespace std;

namespace std {
    class gameRules
    {
    public:
        static void evaluate(int neighbors);
    private:
    };
}
#endif
